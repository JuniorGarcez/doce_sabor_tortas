<div class="container">
  <p style="color: green"><%= notice %></p>

  <%= render @product %>
  <br>
  <%= link_to "Edit this product", edit_product_path(@product), class: 'btn btn-primary btn-sm'%>
  <%= link_to "Back to products", products_path, class: 'btn btn-primary btn-sm' %>
  <%= button_to "Destroy this product", @product, method: :delete, class: 'btn btn-danger btn-sm' %>
</div>
