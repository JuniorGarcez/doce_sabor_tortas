<p style="color: green"><%= notice %></p>

<div id="products" class="container container-index">
  <% @products.each do |product| %>
    <%= render product %>
  <% end %>
</div>

<div class="container">
  <%= link_to "New product", new_product_path, class: 'btn btn-primary btn-sm' %>
</div>
